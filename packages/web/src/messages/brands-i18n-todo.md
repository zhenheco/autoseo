# Brands i18n TODO

Issue #87 added `nav.brands` and `dashboard.brands.*` keys to all 7 UI locale files.

- `zh-TW` and `en-US` have human-written strings.
- `ja-JP`, `ko-KR`, `de-DE`, `es-ES`, and `fr-FR` currently use English placeholders.
- Dispatch Gemini or a translator pass before release copy freeze to localize the five placeholder locale blocks.

Issue #69 added `lp.pain.*` and `lp.how.*` keys to all 7 UI locale files.

- `zh-TW` and `en-US` have human-written LP Pain/How section strings.
- `ja-JP`, `ko-KR`, `de-DE`, `es-ES`, and `fr-FR` currently use English placeholders for the new LP blocks.
- Gemini-dispatch TODO: localize `lp.pain.*` and `lp.how.*` in the five placeholder locales before LP v2 copy freeze.
