# Legacy Plan Grandfather Report

Generated at: pending manual run

Run manually with:

```bash
op run --env-file=<env-file> -- pnpm --filter @seo/web exec tsx scripts/migrate/grandfather-free-users.ts
```

The script updates this report with masked recipient emails, send results, and trial end dates.
