〔方案選單〕# 🧪 完整功能測試清單

本測試清單涵蓋所有近期修復和改進的功能。請依序執行測試，並記錄測試結果。

---

## ✅ 測試狀態圖例

- [ ] 未測試
- [✓] 測試通過
- [✗] 測試失敗

---

## 1. WordPress 連線驗證功能 (P0 - 最高優先級)

### 測試目的

驗證在新增 WordPress 網站時，系統會先測試連線是否成功，才允許儲存設定。

### 測試步驟

#### 1.1 測試正確的 WordPress 連線

- [ ] 1. 前往「網站管理」→「新增網站」
- [ ] 2. 輸入以下資訊：
  - 網站名稱：測試網站
  - 網站 URL：（你的 WordPress 網站 URL）
  - 使用者名稱：（你的 WordPress 使用者名稱）
  - 應用密碼：（正確的 WordPress Application Password）
- [ ] 3. 點擊「儲存」

**預期結果**：

- 系統會嘗試連線到 WordPress
- 連線成功後，顯示成功訊息並導向網站列表
- 新網站的 `wp_enabled` 欄位應為 `true`

#### 1.2 測試錯誤的應用密碼

- [ ] 1. 前往「網站管理」→「新增網站」
- [ ] 2. 輸入資訊（使用錯誤的應用密碼）
- [ ] 3. 點擊「儲存」

**預期結果**：

- 連線驗證失敗
- 顯示錯誤訊息：「WordPress 連線失敗，請檢查網址、使用者名稱和應用密碼是否正確」
- 不會建立網站記錄

#### 1.3 測試錯誤的網站 URL

- [ ] 1. 前往「網站管理」→「新增網站」
- [ ] 2. 輸入無效的 URL（例如：`http://invalid-url-123456.com`）
- [ ] 3. 點擊「儲存」

**預期結果**：

- 連線驗證失敗
- 顯示錯誤訊息
- 不會建立網站記錄

### 檢查點

- [ ] 連線驗證功能運作正常
- [ ] 錯誤處理正確
- [ ] Console 日誌顯示 `[新增網站] WordPress 連線驗證成功` 或失敗訊息

---

## 2. WordPress 文章發佈功能 (P0 - 最高優先級)

### 測試目的

驗證文章能夠正確發佈到 WordPress，並且資料庫記錄正確更新。

### 前置條件

- 已完成「測試 1」並成功新增至少一個 WordPress 網站
- 已有至少一篇生成的文章（狀態為 completed 或 generated）

### 測試步驟

#### 2.1 發佈文章到 WordPress（發布狀態）

- [ ] 1. 前往「文章管理」
- [ ] 2. 選擇一篇已完成的文章
- [ ] 3. 點擊「發布」按鈕
- [ ] 4. 選擇目標網站
- [ ] 5. 選擇發布狀態：「發布」（publish）
- [ ] 6. 確認發布

**預期結果**：

- 顯示發布成功訊息
- 文章狀態更新為 `published`
- 資料庫記錄包含：
  - `wordpress_post_id`（WordPress 文章 ID）
  - `wordpress_post_url`（WordPress 文章 URL）
  - `wordpress_status` = `publish`
  - `published_to_website_id`（目標網站 ID）
  - `published_at` 和 `published_to_website_at` 時間戳記
- 前往 WordPress 後台確認文章已發布

#### 2.2 發佈文章到 WordPress（草稿狀態）

- [ ] 1. 選擇另一篇文章
- [ ] 2. 點擊「發布」
- [ ] 3. 選擇發布狀態：「草稿」（draft）
- [ ] 4. 確認發布

**預期結果**：

- 文章在 WordPress 中顯示為草稿
- `wordpress_status` = `draft`
- 其他欄位正確更新

#### 2.3 發佈包含圖片、分類、標籤的文章

- [ ] 1. 選擇一篇包含精選圖片、分類、標籤的文章
- [ ] 2. 發布到 WordPress

**預期結果**：

- WordPress 文章包含所有內容（圖片、分類、標籤）
- SEO 元數據正確（如果有安裝 Yoast SEO 或 Rank Math）

### 檢查點

- [ ] WordPress API 呼叫成功
- [ ] 資料庫欄位正確更新
- [ ] WordPress 後台可以看到已發布的文章
- [ ] Console 日誌顯示 `[WordPress] 文章發布成功！`

---

## 3. 文章狀態顯示功能 (P1 - 高優先級)

### 測試目的

驗證已發布文章的狀態圖示正確顯示（紅色實心圓圈）。

### 測試步驟

#### 3.1 檢查已發布文章的狀態圖示

- [ ] 1. 前往「文章管理」
- [ ] 2. 觀察已發布文章的狀態圖示

**預期結果**：

- 已發布文章顯示 **紅色實心圓圈** 圖示
- 懸停時顯示 Tooltip：「已發佈到 [網站名稱]」
- 如果是草稿狀態，顯示：「已發佈到 [網站名稱] (草稿)」

#### 3.2 檢查其他狀態的圖示

- [ ] 1. 觀察不同狀態文章的圖示

**預期結果**：

- `completed` / `generated` / `reviewed`：綠色勾勾
- `scheduled`：藍色日曆圖示 + 排程時間
- `processing`：黃色旋轉圖示
- `pending`：灰色時鐘圖示
- `failed`：紅色叉叉圖示

### 檢查點

- [ ] 所有狀態圖示顯示正確
- [ ] Tooltip 資訊正確
- [ ] 圖示顏色符合設計

---

## 4. Credits 扣除功能 (P0 - 最高優先級)

### 測試目的

驗證文章生成時，Credits 正確扣除（非快取情況下）。

### 背景說明

之前診斷發現所有已完成的 jobs 都使用了快取邏輯（`saved_article_id` 存在），導致 `tokenUsage = 0`，因此沒有扣除 Credits。這是正常行為。

### 測試步驟

#### 4.1 測試全新文章生成（非快取）

- [ ] 1. 前往「批次文章生成」
- [ ] 2. 輸入一個**完全沒有生成過的關鍵字**（確保不會觸發快取）
- [ ] 3. 選擇字數（例如：1000 字）
- [ ] 4. 提交生成請求
- [ ] 5. 等待文章生成完成（可能需要數分鐘）

**預期結果**：

- 文章生成完成後，檢查資料庫：

  ```sql
  SELECT * FROM token_usage_logs
  ORDER BY created_at DESC
  LIMIT 5;

  SELECT * FROM token_deduction_records
  ORDER BY created_at DESC
  LIMIT 5;
  ```

- `token_usage_logs` 應該有新記錄，`input_tokens` 和 `output_tokens` 都 > 0
- `token_deduction_records` 應該有新記錄，`tokens_deducted` > 0
- Console 日誌應顯示：
  ```
  [Orchestrator] 💳 Token deduction decision: {
    charged: <非零值>,
    willDeduct: true,
    ...
  }
  ```

#### 4.2 測試快取文章（驗證不會重複扣除）

- [ ] 1. 再次生成**相同關鍵字**的文章
- [ ] 2. 觀察執行時間（應該非常快，< 2 秒）

**預期結果**：

- Console 日誌顯示：
  ```
  [Orchestrator] ⚠️ Using cached article, skipping token deduction {
    articleId: ...,
    jobId: ...,
    reason: 'Article already generated (idempotency check)',
  }
  ```
- 資料庫沒有新增 token 扣除記錄（正確行為）

### 檢查點

- [ ] 非快取文章生成正確扣除 Credits
- [ ] 快取文章不會重複扣除
- [ ] 日誌清楚記錄扣除決策

---

## 5. 文章列表樣式優化 (P2 - 低優先級)

### 測試目的

驗證文章列表中，標題字體比元數據更大更明顯。

### 測試步驟

#### 5.1 檢查文章列表樣式

- [ ] 1. 前往「文章管理」
- [ ] 2. 觀察文章列表中的標題和元數據

**預期結果**：

- 標題使用 `text-base font-semibold`（字體較大且粗體）
- 元數據使用 `text-xs text-muted-foreground`（字體較小且灰色）
- 視覺層級清晰：標題 > 元數據

### 檢查點

- [ ] 標題字體大小正確
- [ ] 元數據字體大小正確
- [ ] 視覺層級符合設計需求

---

## 6. 整合測試

### 測試目的

驗證所有功能整合後的完整流程。

### 測試步驟

#### 6.1 完整流程測試

- [ ] 1. 新增一個 WordPress 網站（測試連線驗證）
- [ ] 2. 生成一篇全新文章（測試 Credits 扣除）
- [ ] 3. 檢查文章列表樣式（測試樣式優化）
- [ ] 4. 發布文章到 WordPress（測試發布功能）
- [ ] 5. 檢查文章狀態圖示（測試狀態顯示）

**預期結果**：

- 所有步驟都成功執行
- 沒有錯誤或異常
- 資料一致性正確

### 檢查點

- [ ] 完整流程順暢
- [ ] 所有功能正常運作
- [ ] 沒有錯誤日誌

---

## 7. 資料庫驗證

### 測試步驟

#### 7.1 檢查 website_configs 表

```sql
SELECT
  id,
  website_name,
  wordpress_url,
  wp_enabled,
  created_at
FROM website_configs
ORDER BY created_at DESC
LIMIT 5;
```

**預期結果**：

- 新增的網站記錄存在
- `wp_enabled` = `true`

#### 7.2 檢查 generated_articles 表

```sql
SELECT
  id,
  title,
  status,
  wordpress_post_id,
  wordpress_post_url,
  wordpress_status,
  published_to_website_id,
  published_at,
  published_to_website_at
FROM generated_articles
WHERE status = 'published'
ORDER BY published_at DESC
LIMIT 5;
```

**預期結果**：

- 已發布文章的所有 WordPress 相關欄位都有值
- 時間戳記正確

#### 7.3 檢查 token_usage_logs 表

```sql
SELECT
  id,
  company_id,
  action_type,
  input_tokens,
  output_tokens,
  total_tokens,
  created_at
FROM token_usage_logs
ORDER BY created_at DESC
LIMIT 5;
```

**預期結果**：

- 有非快取文章生成的記錄
- token 數量合理

#### 7.4 檢查 token_deduction_records 表

```sql
SELECT
  id,
  company_id,
  idempotency_key,
  tokens_deducted,
  tokens_before,
  tokens_after,
  created_at
FROM token_deduction_records
ORDER BY created_at DESC
LIMIT 5;
```

**預期結果**：

- 有對應的扣除記錄
- `tokens_after` = `tokens_before` - `tokens_deducted`

---

## 8. 錯誤處理測試

### 測試步驟

#### 8.1 測試網路錯誤

- [ ] 1. 關閉 WordPress 網站或中斷網路
- [ ] 2. 嘗試發布文章

**預期結果**：

- 顯示友善的錯誤訊息
- 不會建立不完整的資料庫記錄

#### 8.2 測試權限錯誤

- [ ] 1. 使用沒有權限的帳號
- [ ] 2. 嘗試發布文章或新增網站

**預期結果**：

- 顯示「無權限」錯誤訊息
- 操作被拒絕

---

## 📊 測試結果總結

### 功能測試結果

| 測試項目              | 狀態 | 備註 |
| --------------------- | ---- | ---- |
| 1. WordPress 連線驗證 | [ ]  |      |
| 2. WordPress 文章發佈 | [ ]  |      |
| 3. 文章狀態顯示       | [ ]  |      |
| 4. Credits 扣除       | [ ]  |      |
| 5. 文章列表樣式       | [ ]  |      |
| 6. 整合測試           | [ ]  |      |
| 7. 資料庫驗證         | [ ]  |      |
| 8. 錯誤處理           | [ ]  |      |

### 發現的問題

請在此記錄測試過程中發現的任何問題：

1.
2.
3.

---

## 🔍 除錯資訊

### 如何查看 Console 日誌

1. 開啟瀏覽器開發者工具（F12）
2. 前往「Console」標籤
3. 過濾關鍵字：
   - `[新增網站]` - 網站新增相關日誌
   - `[WordPress]` - WordPress API 相關日誌
   - `[Orchestrator]` - 文章生成和 Token 扣除日誌

### 如何查看資料庫

```bash
# 連接到 Supabase 資料庫
source .env.local
psql "$SUPABASE_DB_URL"
```

---

## ✅ 測試完成確認

- [ ] 所有測試項目都已執行
- [ ] 測試結果已記錄
- [ ] 發現的問題已記錄
- [ ] 重大問題已回報

---

**測試人員**：**\*\***\_**\*\***
**測試日期**：**\*\***\_**\*\***
**測試環境**：**\*\***\_**\*\***
